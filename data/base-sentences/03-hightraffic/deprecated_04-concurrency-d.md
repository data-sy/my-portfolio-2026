# Troubleshooting D: 트레이드오프형

## 선착순 쿠폰 동시성 제어 — 락 전략 트레이드오프 분석
| key | value |
|---|---|
| title | 선착순 쿠폰 동시성 제어 — 락 전략 트레이드오프 분석 |
| situation | 선착순 100명 쿠폰 발급 시 동시 1000명 요청으로 초과 발급 발생. 정합성을 보장하면서 성능 저하를 최소화할 락 전략 필요 |
| option_1_name | JVM 락 (synchronized) |
| option_2_name | DB 비관적 락 (SELECT FOR UPDATE) |
| option_3_name | Redis Lua Script |
| c01_name | 분산 환경 |
| c01_opt1 | 단일 서버만 유효 |
| c01_opt2 | 지원 |
| c01_opt3 | 완벽 지원 |
| c02_name | 동시 1000명 |
| c02_opt1 | Scale-out 불가 |
| c02_opt2 | 커넥션 풀 고갈 위험 |
| c02_opt3 | 안정적 처리 |
| c03_name | 데이터 정합성 |
| c03_opt1 | DB 직접 접근 |
| c03_opt2 | 트랜잭션 보장 |
| c03_opt3 | Redis-DB 동기화 필요 |
| c04_name | 추가 인프라 |
| c04_opt1 | 없음 |
| c04_opt2 | 없음 |
| c04_opt3 | Redis 필요 |
| c05_name | DB 부하 |
| c05_opt1 | 없음 |
| c05_opt2 | 커넥션 점유 |
| c05_opt3 | 없음 |
| c06_name | 처리 지연 |
| c06_opt1 | JVM 내 ns 수준 |
| c06_opt2 | 네트워크 + 락 대기 |
| c06_opt3 | 네트워크 1회 (ms 수준) |
| c07_name | 장애 영향 |
| c07_opt1 | JVM 내 한정 |
| c07_opt2 | DB 장애 시 전체 중단 |
| c07_opt3 | Redis 장애 시 발급 불가 |
| final_choice | Redis Lua Script |
| reasoning | 다중 서버 환경에서 DB 커넥션 부담 없이 원자적 처리가 가능. 이미 캐시 용도로 Redis를 사용 중이므로 추가 인프라 비용 없음 |
| insight_1 | 동시성 제어는 "정합성 보장 수준 vs 처리 성능"의 트레이드오프. 락을 강하게 걸수록 정합성은 높아지지만 처리량이 떨어짐. 요구사항에 맞는 수준을 선택해야 함 |
| insight_2 | 기술 선택은 현재 인프라 맥락에 영향받음. Redis를 이미 사용 중이었기에 도입 비용이 낮았고, 그렇지 않았다면 DB 비관적 락이 더 현실적인 선택이었을 수 있음 |
| followup_q1 | Redis 장애 시 쿠폰 발급은 어떻게 되나? |
| followup_q2 | DB 비관적 락을 선택했다면 커넥션 풀 문제를 어떻게 완화했을까? |

<!--
c01~c07 우선순위 순서 (포트폴리오 게재용):
  소거법:    c01 분산 환경, c02 동시 1000명
  약점 인정: c03 데이터 정합성, c04 추가 인프라, c07 장애 영향
  장점 근거: c05 DB 부하, c06 처리 지연

  스토리 흐름:
  c01~c02에서 JVM 락·DB 락 소거
  → c03~c04에서 Redis 약점 인정
  → c05~c06에서 그럼에도 선택한 장점 근거
  → c07에서 리스크 인지까지 마무리

탈락 항목 (비교표 미포함, 면접 대비용):
  - 구현 복잡도: "쉬워서 선택했다"는 인상 위험. 선택 근거로 약함
  - 테스트 용이성: 비교표 한 칸보다 실제 테스트 코드로 증명하는 게 효과적
  - 디버깅 난이도: 운영 경험 없이 언급하면 "아는 척"으로 보일 위험
  - 장애 복구 전략: 강력한 포인트지만 별도 트러블슈팅 문서로 깊게 다루는 게 임팩트 있음
  - Scale-out 대응: c01 분산 환경과 같은 장점의 다른 표현. 중복감

option_3 이름 변경: "Redis 분산락 + Lua Script" → "Redis Lua Script"
  - 이 시나리오에서 Lua Script의 EVAL 원자성만으로 수량 확인+차감이 가능
  - 별도 분산락(Redisson 등)은 더 복잡한 비즈니스 로직용
  - 면접에서 "분산락과 Lua Script 차이"를 물어보면 아래 맥락으로 대응

insight_1 방향: 정합성 vs 성능 트레이드오프 판단 기준
insight_1 예상 꼬리질문: 정합성이 깨져도 되는 상황이 있나? / 낙관적 락은 왜 고려하지 않았나? / 처리량 기준은 어떻게 정했나?

insight_2 방향: 인프라 맥락 기반 기술 선택
insight_2 예상 꼬리질문: Redis 없는 환경이었다면? / 새 인프라 도입 시 고려할 점은? / 인프라 의존성이 늘어나는 것의 단점은?

followup_a1: Redis 장애 시 DB 비관적 락으로 폴백하는 이중 전략 고려. 또는 서킷 브레이커 패턴으로 장애 감지 후 빠른 실패 처리. 선착순 특성상 짧은 이벤트이므로 Redis 고가용성(Sentinel/Cluster) 구성이 우선.
followup_a2: 커넥션 풀 사이즈 확대 + 락 타임아웃 설정으로 최소한의 대기. 또는 큐 기반 순차 처리로 동시 접근 자체를 줄이는 방식. 하지만 1000명 동시 요청 시 커넥션 풀 확대에는 한계가 있음.
-->


<!--
## 트레이드오프 맥락 (면접 대비용)

옵션 선택 프레임:
- 락 위치를 애플리케이션 → DB → 외부 저장소로 계층별 확장하며 검토
- JVM 락(애플리케이션 단) → DB 비관적 락(DB 단) → Redis Lua Script(외부 인메모리 DB 단)
- 단순 나열이 아닌 계층별 체계적 탐색

상황에서 핵심 제약 조건:
- 제약 1: 다중 서버 배포 환경 → JVM 락 불가
- 제약 2: 동시 1000명 요청 → 높은 동시성 환경
- 제약 3: 정확히 100명만 발급 → 정합성 필수

각 선택지를 고려한 이유:
- synchronized: 가장 단순하지만 분산 환경 미지원으로 탈락
- DB 비관적 락: 분산 환경 지원하지만 커넥션 점유 문제
- Redis Lua Script: DB 부하 분리, 원자적 처리 가능

최종 선택의 결정적 근거:
- DB 비관적 락 대비: 커넥션 풀 고갈 위험 없음
- 이미 Redis 인프라 존재: 추가 도입 비용 최소
- Lua Script: 별도 분산락 없이도 원자적 수량 관리 가능

만약 상황이 달랐다면:
- Redis 미사용 환경 → DB 비관적 락 + 커넥션 풀 튜닝
- 동시 요청이 10명 이하 → synchronized로 충분
- 정합성보다 처리량 우선 → 낙관적 락 + 재시도
-->
